name: ScheduleTest

on:
  push:
   branches:
     - main 
  schedule:
   - cron: '*/2 * * * *'

jobs:
  Job1:
   runs-on: ubuntu-latest
   steps:
   - name: Scheduling test
     shell: bash
     run: |
      echo "This is testing"
